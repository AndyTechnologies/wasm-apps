# Fuente: Compiler README

**Ruta**: `packages/compiler/README.md`
**Propósito**: Documentación del paquete `@wasm-apps/compiler`.

## Contenido extraído

- API principal: `compileWasm()` con opciones y resultado
- Caché en dos niveles (LRU en memoria + disco)
- CLI build y watch con opciones
- Gestión de caché: `getCompileCacheInfo()`, `clearCompileCache()`
- Dependencias

Ver [[entities/compiler|Compilador]] para la síntesis.
