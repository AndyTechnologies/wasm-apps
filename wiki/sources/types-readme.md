# Fuente: Types README

**Ruta**: `packages/types/README.md`
**Propósito**: Documentación del paquete `@wasm-apps/types`.

## Contenido extraído

- Tipos principales (WasmExport, WasmImport, WasmModuleInfo, ResolvedLink, etc.)
- Clases de error (CompilerError, LinkerError, DownloadError, CMakeError, ConfigError, ZigError)
- Logger con colores
- Utilidades: `colorizeByStatus()`, `formatBytes()`
- Dependencia: picocolors

Ver [[entities/types|Tipos Compartidos]] para la síntesis.
