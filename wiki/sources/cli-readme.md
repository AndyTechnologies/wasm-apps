# Fuente: CLI README

**Ruta**: `packages/cli/README.md`
**Propósito**: Documentación del paquete `@wasm-apps/cli`.

## Contenido extraído

- Comandos: init, build, setup, cache info, cache clear
- API: `resolveConfig()`, `initProject()`, `buildProject()`, `devCommand()`, `cacheInfo()`, `clearCache()`
- Opciones de build con tabla detallada
- Configuración `wapp.json` completa con tipos y defaults
- Dependencias del paquete

Ver [[entities/cli|CLI (wapp)]] para la síntesis.
