# Fuente: Referencia de CLI

**Ruta**: `docs/reference/cli.md`
**Propósito**: Referencia detallada de flags y subcomandos del CLI.

## Contenido extraído

- `wapp init [dir]` — crea wapp.json
- `wapp build [options]` — tabla completa de opciones (output, target, entry, module-matching, source-dir, out-dir, release, optimize-level, shrink-level, wasi)
- `wapp setup` — descarga Wasmtime C-API con reanudación HTTP range
- `wapp cache info/clear` — gestión de caché
- Compiler CLI: `build` y `watch` con opciones
- Linker CLI: `build`, `watch`, `setup`, `status`, `cache info/clear`

Ver [[entities/cli|CLI (wapp)]] para la síntesis.
