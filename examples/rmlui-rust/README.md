# RmlUI Rust Example

Demostración de RmlUI desde Rust WASM. Usa FFI directo a las funciones host
del plugin `rmlui-plugin`. Las bindings de Rust se generan de `rmlui-abi.h`.

## Build & Run

```bash
cd ../..
pnpm build:example -- examples/rmlui-rust
```
