# Wasmtime C-API path (opcional, por defecto se descarga automáticamente)
# WASMTIME_PATH=/ruta/personalizada/wasmtime

# Nivel de log (debug, info, warn, error)
# LOG_LEVEL=info

# Target de compilación por defecto (ej: x86_64-linux-gnu, aarch64-macos)
# BUILD_TARGET=native
