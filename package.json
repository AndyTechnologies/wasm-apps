{
  "name": "wasm-apps",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "pnpm -r build",
    "release": "pnpm build && pnpm test && pnpm -r publish",
    
    "compiler": "node ./packages/compiler/dist/cli.js",
    "linker": "node ./packages/linker/dist/cli.js",
    "cli": "node ./packages/cli/dist/cli.js",

    "test:build": "pnpm cli build --source-dir examples -o wasm-out-app --out-dir wasm-out",
    "test:exec": "node scripts/run-built-bin.mjs",
    "test": "pnpm test:build && pnpm test:exec"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "typescript": "^7.0.2",
    "@types/node": "^26.1.1"
  }
}